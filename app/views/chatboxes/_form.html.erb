<%= form_with(model: chatbox) do |form| %>
  <% if chatbox.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chatbox.errors.count, "error") %> prohibited this chatbox from being saved:</h2>

      <ul>
        <% chatbox.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :is_private %>
    <%= form.check_box :is_private %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
